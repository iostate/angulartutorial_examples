Assignment 5

Instantiate two instances of a Services component. 
- One component for the inactive users.
- One component for the active users.

Services Component Fields
- Other Service
	- Clicking inactive will send to active component


Counter Component:

Create a counter service.
Instantiate the counter service in accounts.service.ts
Keep track of the counter via calling .length
